<?xml version="1.0" encoding="utf-8"?>
<!--
    Android blocks plain-HTTP (cleartext) traffic by default since API 28.
    MaterialViewerActivity still calls a local PHP backend over HTTP on the
    dev LAN, so cleartext is explicitly allowed for just that host instead
    of the whole app — everything else (Supabase) stays HTTPS-only.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">192.168.1.109</domain>
    </domain-config>
</network-security-config>
