<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <!-- Frosted-glass look: a translucent white sheen (diagonal, brighter at
         top-left) over whatever maroon background sits behind it, edged with
         a faint light border to read as a glass panel rather than a flat tint. -->
    <gradient
        android:angle="135"
        android:startColor="#40FFFFFF"
        android:endColor="#1AFFFFFF" />
    <corners android:radius="28dp" />
    <stroke
        android:width="1dp"
        android:color="#59FFFFFF" />
</shape>
